version: '3.6'
services:
  app:
    container_name: psp-service
    build: .
    volumes:
      - .:/app
    env_file:
      - .env
    networks:
      - general
    ports:
      - 8089:80

networks:
  general:
    driver: bridge
